자바에서 클래스는 프로그램에서 사용될 객체의 틀을 정의하며, 객체를 생성하기 위한 설계도 역할을 한다.

클래스는 객체의 상태를 나타내는 필드(field)와 객체의 행동을 나타내는 메소드(method)로 구성됩니다.

즉, 필드(field)란 클래스에 포함된 변수(variable)를 의미합니다.

또한, 메소드(method)란 어떠한 특정 작업을 수행하기 위한 명령문의 집합이라 할 수 있습니다.

자바 프로그램은 최소 한개의 클래스로 구성된다.

그리고 그 클래스는 한 개 이상의 필드(field) 또는 메서드(method)로 구성된다.

---

### 예시1

위에서 보면 맨 상단에 학생은학생 클래스임을 뜻하며
중간에는학생이 가지고 있는 속성(변수) 를 나타내며
하단에는학생이 하는일 행위(메소드) 를 나타낸다.
위에 다이어그램을 코드로 나타내면 아래와 같다.

```java
public class Student{

   private id;
   private name;
   private gender;
   private major;
   private course;

   public void regCourse(){

   }

}
```

---

### 예시2

기본 구조는 다음과 같다.

```java
public class Test {  // 클래스 선언 : 어떠한 객체의 변수, 메서드의 집합

	int a = 1;  // 필드 부분 : 객체 데이터가 저장되는 곳
    
	public void method() {  // 메서드 부분 : 객체의 동작을 수행하는 부분
		
		System.out.println(a);
		
	}
}
```
